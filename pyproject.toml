[project]
name = "nebius-academy-ddp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasets>=4.8.5",
    "torch>=2.11.0",
    "transformers[torch]>=5.8.0",
]
